/*
 * system_prompt.h - Build system prompt with environment context
 *
 * Functions to construct the complete system prompt including:
 * - Environment information (platform, OS, git status)
 * - Working directories
 * - KLAWED.md contents
 * - SKILLS directory information
 */

#ifndef CONTEXT_SYSTEM_PROMPT_H
#define CONTEXT_SYSTEM_PROMPT_H

#include "../klawed_internal.h"

// Note: build_system_prompt is declared in klawed_internal.h to avoid redundant declarations

#endif /* CONTEXT_SYSTEM_PROMPT_H */
