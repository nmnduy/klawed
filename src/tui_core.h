/*
 * TUI Core Initialization and Cleanup
 *
 * Handles core TUI lifecycle operations:
 * - Initialization (ncurses setup, color scheme, window creation)
 * - Cleanup (resource deallocation, ncurses teardown)
 * - Suspend/Resume (for external commands)
 * - Startup banner and tips
 * - TODO list and subagent status rendering
 *
 * This module contains the implementations for functions declared in tui.h.
 * No additional public API is exposed - all functions are already declared in tui.h.
 */

#ifndef TUI_CORE_H
#define TUI_CORE_H

// This header exists for organizational purposes and to document the module.
// All public functions are declared in tui.h.
// This file is included by tui_core.c for module cohesion documentation.

#endif // TUI_CORE_H
