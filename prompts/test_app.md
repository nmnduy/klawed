use scripts/ws_bridge_server.py to test our application:\n\nlogin with user nmnduy@gmail.com\n\n- ask the AI what files we have in the workspace top level\n- ask the ai how much we got in our solana wallet. if we dont have one, create one and ask it to print the address\n\ncheck for any irregularities in the responses, or irregulartities in the klawed db file and in chat session db file\n\nthere can be other agents testing the same quarkus server at 9090, so perform your validating by checking db files in your session only. that said, you need to spawn your own instance of scripts/ws_bridge_server.ts on a separate port that isn't used by another agent.
