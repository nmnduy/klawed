{
  "name": "invoicesurf-css",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node -e \"const fs = require('fs'); const path = require('path'); const assetsDir = path.join(__dirname, 'src/main/resources/META-INF/resources/assets'); try { fs.rmSync(assetsDir, { recursive: true, force: true }); } catch(e) {}\"",
    "build": "vite build",
    "watch": "vite build --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "tailwindcss-animate": "^1.0.7",
    "vite": "^5.0.8"
  }
}
